name: cgpa_calculator
description: A simple CGPA calculator built with Flutter.
publish_to: "none"
version: 0.0.1
environment:
  sdk: ">=2.17.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  google_fonts: ^4.0.3
  cupertino_icons: ^1.0.2

flutter:
  uses-material-design: true
